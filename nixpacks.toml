[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "python3 -m venv /opt/venv",
  "/opt/venv/bin/pip install -r telegram-bot/requirements.txt"
]

[start]
cmd = "/opt/venv/bin/python telegram-bot/bot.py"
